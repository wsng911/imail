services:
  imall:
    image: wsng911/imail:latest
    restart: unless-stopped
    ports:
      - "30000:3000"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - DATA_DIR=/app/data
